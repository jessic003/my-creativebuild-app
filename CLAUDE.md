# Claude Instructions

- After every change, run: `git add . && git commit -m "[claude] update" && git push`
- Use Tailwind for all styling
- Keep components in `/src/components`
- Main page is `/src/App.jsx`
